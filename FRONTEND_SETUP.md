# 비회원 게시판 - 프론트엔드 실행 가이드

## 개요
비회원 게시판의 프론트엔드는 순수 HTML, CSS, JavaScript로 구성된 SPA(Single Page Application)입니다.
프레임워크에 의존하지 않아 별도의 빌드 과정 없이 직접 브라우저에서 실행할 수 있습니다.

---

## 파일 구성

```
day02/
├── board.html                 # 프론트엔드 메인 파일 (모든 기능 포함)
├── API_ENDPOINTS.md          # 백엔드 API 문서
├── BOARD_REQUIREMENTS.md     # 기능 명세서
└── FRONTEND_SETUP.md         # 이 파일
```

---

## 프론트엔드 파일 설명

### board.html
- **크기**: 약 20KB
- **기능**:
  - 게시글 목록 페이지 (페이지네이션)
  - 게시글 상세 페이지 (댓글 포함)
  - 게시글 작성/수정 페이지
  - 모든 CRUD 작업 지원
  - 다크 테마 자동 지원
  - 반응형 모바일 디자인

---

## 실행 방법

### 방법 1: 직접 열기 (가장 간단)

1. 브라우저에서 `board.html` 파일을 직접 열기:
   ```bash
   # Windows
   start board.html
   
   # Mac
   open board.html
   
   # Linux
   xdg-open board.html
   ```

   또는 파일 탐색기에서 `board.html`을 더블클릭합니다.

### 방법 2: 간단한 HTTP 서버 사용 (권장)

#### Python 3 사용:
```bash
# Windows PowerShell
cd C:\Users\SAMSUNG\claude_1900_mky\workspace\claude2\day02
python -m http.server 3000
```

그 후 브라우저에서 `http://localhost:3000/board.html` 열기

#### Python 2 사용:
```bash
cd C:\Users\SAMSUNG\claude_1900_mky\workspace\claude2\day02
python -m SimpleHTTPServer 3000
```

#### Node.js 사용:
```bash
# http-server 설치 (처음 한 번만)
npm install -g http-server

# 서버 실행
cd C:\Users\SAMSUNG\claude_1900_mky\workspace\claude2\day02
http-server -p 3000
```

그 후 브라우저에서 `http://localhost:3000/board.html` 열기

---

## 백엔드 서버 실행

프론트엔드가 제대로 작동하려면 백엔드 서버가 실행 중이어야 합니다.

### 백엔드 서버 시작:

```bash
# 프로젝트 루트 디렉토리에서 (day02 폴더에서)
node server.js
# 또는
npm start
```

**서버 확인**:
- 백엔드 API: http://localhost:8000/api/health
- 브라우저 콘솔에서 네트워크 탭을 보면 API 요청이 표시됩니다.

---

## 사용 방법

### 1. 게시글 목록 페이지 (초기 화면)
- 최신 게시글 10개씩 표시
- **새 글 작성** 버튼으로 새 게시글 작성
- 게시글을 클릭하면 상세 페이지로 이동
- 하단 페이지네이션으로 이전/다음 페이지 이동

### 2. 게시글 작성/수정
- **필수 항목**:
  - 제목 (1-200자)
  - 내용 (1자 이상)
  - 작성자명 (1-50자)
  - 비밀번호 (최소 4자)
- **저장** 버튼으로 게시글 저장
- **취소** 버튼으로 목록으로 돌아가기

### 3. 게시글 상세 페이지
- 게시글 제목, 내용, 작성자, 작성일 표시
- **수정** 버튼:
  - 클릭 시 비밀번호 입력
  - 비밀번호 확인 후 수정 폼으로 이동
- **삭제** 버튼:
  - 클릭 시 비밀번호 입력
  - 비밀번호 확인 후 게시글 삭제
  - 게시글의 모든 댓글도 함께 삭제됨

### 4. 댓글 기능
- **댓글 작성**:
  - 작성자명, 댓글 내용, 비밀번호 입력
  - **댓글 작성** 버튼
- **댓글 수정**:
  - 각 댓글의 **수정** 버튼 클릭
  - 비밀번호 입력 → 새 내용 입력
- **댓글 삭제**:
  - 각 댓글의 **삭제** 버튼 클릭
  - 비밀번호 입력 → 삭제 확인

---

## 브라우저 호환성

- **권장**: Chrome, Firefox, Safari, Edge (최신 버전)
- **최소 요구사항**: ES6 지원하는 모던 브라우저
  - Chrome 51+
  - Firefox 54+
  - Safari 10+
  - Edge 15+

---

## 네트워크 요청 목록

### 게시글 관련
- `GET /api/posts?page=1&limit=10` - 게시글 목록 조회
- `GET /api/posts/{id}` - 게시글 상세 조회 (댓글 포함)
- `POST /api/posts` - 게시글 작성
- `PUT /api/posts/{id}` - 게시글 수정
- `DELETE /api/posts/{id}` - 게시글 삭제

### 댓글 관련
- `POST /api/posts/{postId}/comments` - 댓글 작성
- `PUT /api/comments/{id}` - 댓글 수정
- `DELETE /api/comments/{id}` - 댓글 삭제

모든 요청은 `Content-Type: application/json` 헤더를 사용합니다.

---

## 테스트 시나리오

### 기본 테스트
1. 브라우저 콘솔 열기 (F12 또는 Ctrl+Shift+I)
2. **Network** 탭에서 API 요청 확인
3. 게시글 목록 로드 확인
4. 새 게시글 작성
5. 게시글 상세 페이지 진입
6. 댓글 작성
7. 댓글 수정/삭제
8. 게시글 수정
9. 게시글 삭제

### 에러 처리 테스트
- 백엔드 서버를 종료한 후 프론트엔드 작동 → 에러 메시지 표시 확인
- 잘못된 비밀번호 입력 → 401 에러 메시지 표시 확인
- 빈 제목 또는 내용으로 작성 시도 → 유효성 검사 메시지 표시 확인

### 반응형 디자인 테스트
- 브라우저 너비를 줄여서 모바일 레이아웃 확인
- 버튼과 입력 필드의 크기 확인
- 텍스트 가독성 확인

---

## 트러블슈팅

### 1. "서버에 연결할 수 없습니다" 에러
**원인**: 백엔드 서버가 실행 중이지 않음
**해결**:
```bash
node server.js
# 또는
npm start
```

### 2. CORS 에러 (Network 탭에서 `Access to XMLHttpRequest...` 에러)
**원인**: 백엔드에 CORS 설정이 없음
**확인**:
- `server.js`에 다음 코드가 있는지 확인:
```javascript
app.use(cors());
```

### 3. 게시글/댓글이 저장되지 않음
**원인**: 비밀번호가 4자 미만, 또는 입력값이 제약조건을 만족하지 않음
**확인**:
- 브라우저 콘솔의 에러 메시지 확인
- 입력값의 길이 및 형식 재확인

### 4. 다크 테마가 적용되지 않음
**해결**:
- 브라우저 설정에서 다크 모드 활성화
- 또는 OS 설정에서 다크 모드 활성화 (Windows 11: 설정 → 개인화 → 색)

### 5. 페이지 새로고침 후 페이지가 사라짐
**원인**: 정상 작동입니다. SPA이므로 항상 목록 페이지로 돌아갑니다.
**해결**: 필요하면 localStorage를 사용하여 마지막 열람 페이지 저장 가능

---

## 성능 최적화

현재 구현:
- 단일 HTML 파일 (20KB)
- 외부 라이브러리 없음
- 압축되지 않은 JavaScript (최소화 가능)

### 선택적 최적화
- HTML 압축 (gzip)
- JavaScript 축소 (minify)
- CSS 분리 및 최적화
- 이미지 캐싱 설정

---

## 확장 기능 제안

프로덕션 배포 시 추가 고려사항:

1. **인증 강화**:
   - HTTPS 필수 (비밀번호 전송)
   - Rate limiting (반복 삭제 방지)
   - 토큰 기반 인증 (선택)

2. **UI/UX 개선**:
   - 로딩 스피너
   - 더블클릭 방지
   - 자동 저장 기능
   - 실시간 댓글 알림

3. **데이터 관리**:
   - 로컬 스토리지 활용
   - 검색 기능
   - 정렬 옵션
   - 필터링 기능

4. **접근성**:
   - 스크린 리더 지원 개선
   - 키보드 네비게이션 강화
   - WAI-ARIA 레이블 추가

---

## 라이센스 및 저작권
이 프로젝트는 학습 목적으로 제작되었습니다.

## 작성 일자
2024-08-22

## 마지막 수정 일자
2026-08-22
